name: executor-union-all
description: >
  Execute UNION ALL as concatenation without de-duplication.

story_points: 2

acceptence_critria:
  - UNION ALL returns rows from left then right without de-duplication
  - Row ordering preserves operand order
  - Unit tests added for this task
deppends_on:
  - task_00059
  - task_00018
