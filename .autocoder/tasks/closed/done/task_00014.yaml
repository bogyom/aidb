name: ast-validator
description: >
  Validate parsed AST nodes for semantic correctness before planning (e.g.,
  table existence, column existence).

story_points: 5

acceptence_critria:
  - Validator checks table and column references
  - Validator rejects unsupported constructs
  - Validator integrated into execute path

  - Unit tests added for this task
deppends_on:
  - task_00013
